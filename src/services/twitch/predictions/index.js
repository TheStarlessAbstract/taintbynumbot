const getPredictions = require("./getPredictions");
const createPrediction = require("./createPrediction");
const cancelPrediction = require("./cancelPrediction");

module.exports = { getPredictions, createPrediction, cancelPrediction };
